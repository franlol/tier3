import styled from 'styled-components';

export const Nav = styled.nav`
  display: flex;
  flex-direction: row;

  margin: 40px 0 20px;
  
  & ul {
    display: flex;
    flex-direction: row;

    margin: 0 auto;
    list-style: none;
  }
`;
