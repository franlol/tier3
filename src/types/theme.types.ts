enum DARK_MODE {
  background = '#232931',
  card = '#393e46',
  header = '#4ecca3',
  text = '#eeeeee'
}

export enum THEME {
  background = DARK_MODE.background,
  card = DARK_MODE.card,
  header = DARK_MODE.header,
  text = DARK_MODE.text,
}
